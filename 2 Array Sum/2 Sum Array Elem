var twoSum = function(nums, target) {
    let tar = 0;
    for(let i=0; i<=nums.length; i++){
        for(let j=i+1; j<=nums.length; j++){
        tar = nums[i] + nums[i+1];
        if (tar === target){
            return [i,j];
        }
    }}}
    
};